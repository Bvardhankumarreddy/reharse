apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Each project gets its own namespace in its own cluster
namespace: karigari

resources:
  - namespace.yaml
  # Add karigari-specific manifests here as the project grows
  # - ../../base   # uncomment if karigari shares the same app stack

# Cluster-specific overrides
# patches:
#   - patch: |-
#       ...
